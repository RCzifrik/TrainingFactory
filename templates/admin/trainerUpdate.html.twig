{% extends 'baseAdmin.html.twig' %}
{% block body %}
    <div class="container-lg my-4">
        <h1 class="text-center">add trainers</h1>
        <div class="row">
            <div class="col-2"></div>
            <div class="col-8">
                {{ form_start(form) }}
                {{ form_end(form) }}
            </div>
            <div class="col-2"></div>
        </div>
    </div>
{% endblock %}